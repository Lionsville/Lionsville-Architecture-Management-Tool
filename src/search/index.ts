/**
 * One search over everything: elements, their documentation, and the decisions
 * about them. `⌘K` opens it; `⌘F` is the element-only one on the canvas.
 */
export { searchAll } from './search'
export type { SearchHit } from './search'
export { searchElements } from './elementSearch'
export { GlobalSearchDialog } from './ui/GlobalSearchDialog'
export { ElementSearchDialog } from './ui/ElementSearchDialog'
